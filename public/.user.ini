; Ocultar informações do PHP
expose_php = Off

; Ocultar headers do servidor
session.name = "app_session"

; Desabilitar headers desnecessários
auto_prepend_file =
auto_append_file =

; Configurações de segurança
session.cookie_httponly = On
session.cookie_secure = Off
session.use_strict_mode = On
